const userService = require('./user.service');
const deckeService = require('./deck.service');
const cardService = require('./card.service');

module.exports = { userService, deckeService, cardService };
