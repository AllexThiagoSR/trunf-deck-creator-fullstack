const userController = require('./user.controller');
const deckController = require('./deck.controller');
const cardController = require('./card.controller');

module.exports = { userController, deckController, cardController };