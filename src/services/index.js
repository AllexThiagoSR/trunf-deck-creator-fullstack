const userService = require('./user.service');
const deckService = require('./deck.service');
const cardService = require('./card.service');

module.exports = { userService, deckService, cardService };
